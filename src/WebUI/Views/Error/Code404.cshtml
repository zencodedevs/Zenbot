@{
    ViewData["Title"] = "Not Found";
    Layout = "_ErrorLayout";
}

<h2 class="mb-3 h4 text-danger">@ViewData["Title"]</h2>
<img class="w-100" src="~/img/error/not-found.svg" alt="@ViewData["Title"]" />
<p class="mt-3 mb-0 h5 text-danger">The page you are looking for, not found!</p>
